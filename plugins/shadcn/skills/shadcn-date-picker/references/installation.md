# Date Picker — Installation

## Required Components

The Date Picker requires both the `Popover` and `Calendar` components:

```bash
npx shadcn@latest add popover
npx shadcn@latest add calendar
```

The `Calendar` component depends on `react-day-picker` and `date-fns`:

```bash
npm install react-day-picker date-fns
```

## Links

- React DayPicker docs: https://react-day-picker.js.org

---

_Source: `/tmp/shadcn-repo/apps/v4/content/docs/components/radix/date-picker.mdx`_
