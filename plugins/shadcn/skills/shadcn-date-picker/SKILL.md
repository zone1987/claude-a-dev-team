---
name: shadcn-date-picker
description: >
  shadcn/ui Date Picker guide — composed from Popover + Calendar (react-day-picker).
  Single date, date range, with presets. Triggers: date picker, shadcn date picker,
  Datumsauswahl, Datumsfeld, react-day-picker, Calendar Popover composition,
  date range picker, date with presets, DatePickerDemo, date-fns format.
---

# shadcn/ui Date Picker

Use this skill whenever the user works with the shadcn/ui Date Picker pattern.

The Date Picker has no own `ui/*.tsx` source file — it is composed from `Popover` + `Calendar` components.

## References

- `references/installation.md` — Required dependencies
- `references/guide.md` — Composition pattern and usage guide
- `references/examples.md` — All examples with full code (basic, presets, range)
