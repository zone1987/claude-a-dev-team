# shadcn-empty

The `Empty` component displays a structured empty state with optional icon/avatar media, title, description, and call-to-action content. No external dependencies — pure Tailwind + CVA.

## Quick start

```bash
npx shadcn@latest add empty
```

## References

- `EMPTY-INSTALLATION.md` — CLI + manual install (no external deps)
- `EMPTY-SOURCE.md` — complete source code
- `EMPTY-API.md` — sub-component props, EmptyMedia variants
- `EMPTY-EXAMPLES.md` — demo, outline, avatar, input-group examples
