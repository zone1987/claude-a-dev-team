# shadcn-dialog

The `Dialog` component creates a modal window overlaid on the primary window. Built on Radix UI (new-york-v4) or Base UI. Supports optional close button, sticky footer, scrollable content, and RTL.

## Sub-components

`Dialog` · `DialogTrigger` · `DialogPortal` · `DialogOverlay` · `DialogClose` · `DialogContent` · `DialogHeader` · `DialogFooter` · `DialogTitle` · `DialogDescription`

## Quick start

```bash
npx shadcn@latest add dialog
```

## References

- `DIALOG-INSTALLATION.md` — CLI + manual install, dependencies
- `DIALOG-SOURCE.md` — complete source code (Radix, Base UI, Radix-nova variant)
- `DIALOG-API.md` — all sub-component props as tables, composition tree
- `DIALOG-EXAMPLES.md` — demo, close button, dialog-in-dropdown examples
- `DIALOG-BASE-VS-RADIX.md` — differences between Base UI and Radix UI implementations
