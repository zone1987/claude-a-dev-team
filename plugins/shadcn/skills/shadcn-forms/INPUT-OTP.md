# shadcn-input-otp

The `InputOTP` component provides an accessible one-time password input built on [input-otp](https://github.com/guilhermerodz/input-otp). Slots are rendered individually and grouped. Supports copy-paste, numeric/alphanumeric patterns, controlled state, and RTL.

## Quick start

```bash
npx shadcn@latest add input-otp
```

## References

- `INPUT-OTP-INSTALLATION.md` — CLI + manual install (requires `input-otp` package)
- `INPUT-OTP-SOURCE.md` — complete source code
- `INPUT-OTP-API.md` — all sub-component props, pattern constants, composition tree
- `INPUT-OTP-EXAMPLES.md` — demo, separator, controlled, pattern examples
