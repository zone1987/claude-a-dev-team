# shadcn-input-group

The `InputGroup` wraps inputs and addons in a unified focus-managed container. Addons (icons, text, buttons) can be placed at four positions via the `align` prop. `InputGroupInput` and `InputGroupTextarea` replace the plain `Input`/`Textarea` inside groups. Uses CSS `has-[]` selectors for focus and error state propagation.

## Quick start

```bash
npx shadcn@latest add input-group
```

## References

- `INPUT-GROUP-INSTALLATION.md` — CLI + manual install (requires button, input, textarea)
- `INPUT-GROUP-SOURCE.md` — complete source code
- `INPUT-GROUP-API.md` — all sub-component props, alignment system
- `INPUT-GROUP-EXAMPLES.md` — demo, icon, text, button, dropdown, spinner, textarea examples
