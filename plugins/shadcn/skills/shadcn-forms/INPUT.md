# shadcn-input

The `Input` component is a styled HTML `<input>` with consistent focus ring, error state (`aria-invalid`), file input support, and disabled styling. No external dependencies beyond Tailwind CSS.

## Quick start

```bash
npx shadcn@latest add input
```

## References

- `INPUT-INSTALLATION.md` — CLI + manual install (no deps)
- `INPUT-SOURCE.md` — complete source code
- `INPUT-API.md` — props, styling notes
- `INPUT-EXAMPLES.md` — demo, with label, with button, file input, disabled, with text
