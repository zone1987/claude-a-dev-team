# shadcn-field

The `Field` family composes accessible form fields. `Field` provides orientation variants, `FieldError` handles validation (supports `errors` array from react-hook-form and Standard Schema), `FieldSeparator` divides sections, and `FieldSet`/`FieldLegend` wrap semantically grouped fields.

## Quick start

```bash
npx shadcn@latest add field
```

## References

- `FIELD-INSTALLATION.md` — CLI + manual install (no external deps)
- `FIELD-SOURCE.md` — complete source code
- `FIELD-API.md` — all sub-component props as tables, composition trees, accessibility notes
- `FIELD-EXAMPLES.md` — payment form demo, checkbox/switch fields, choice cards, fieldset
