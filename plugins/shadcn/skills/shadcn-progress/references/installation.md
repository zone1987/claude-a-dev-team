# Progress — Installation

## CLI

```bash
npx shadcn@latest add progress
```

## Manual (Radix variant — new-york-v4 default)

1. Install dependency:

```bash
npm install radix-ui
# or specifically:
npm install @radix-ui/react-progress
```

2. Copy the source into `components/ui/progress.tsx`.

## Manual (Base UI variant)

1. Install dependency:

```bash
npm install @base-ui/react
```

2. Use the base variant source (see `base-vs-radix.md`).

---
Source: `content/docs/components/base/progress.mdx`, `content/docs/components/radix/progress.mdx`
