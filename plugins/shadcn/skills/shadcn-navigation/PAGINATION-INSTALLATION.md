# Pagination — Installation

## CLI

```bash
npx shadcn@latest add pagination
```

## Manual

Copy the source into `components/ui/pagination.tsx`. Requires:
- `lucide-react` for icons
- `@/components/ui/button` for `buttonVariants`
- `@/lib/utils` for `cn`

---
Source: `content/docs/components/base/pagination.mdx`
