# config/routes.yaml (`ResubmissionAppServer/config/routes.yaml`)

## Zweck
Routing-Einstieg: importiert die Controller-Routen über Attribute (`#[Route]`).

## Inhalt
- `controllers: resource: routing.controllers` (Attribut-Routen aus `src/Controller/`).

## Bezüge
`src/Controller/*`, `config/routes/*`.
