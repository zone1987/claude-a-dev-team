# config/packages/http_discovery.yaml (`ResubmissionAppServer/config/packages/http_discovery.yaml`)

## Zweck
Konfiguration der PSR-HTTP-Discovery (php-http/discovery) — legt die zu nutzenden HTTP-Client-/Message-Factory-Implementierungen fest. Standard/Boilerplate.

## Bezüge
`shopware/app-bundle` / Symfony HttpClient (Boilerplate).
