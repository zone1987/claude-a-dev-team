# MainController (`ResubmissionAppServer/src/Controller/MainController.php`)

## Zweck
Landing-Page des AppServers.

## Typ
- Namespace `App\Controller`, `#[AsController]`, `extends AbstractController`.

## Routen / Methoden
- `index` — `GET /` → `index.html.twig`.

## Bezüge
`templates/index.html.twig`.
