# config/packages/framework.yaml (`ResubmissionAppServer/config/packages/framework.yaml`)

## Zweck
Symfony-Framework-Grundkonfiguration (secret, http_method_override, Session/PHP-Errors etc. — Standard/Boilerplate).

## Bezüge
Symfony FrameworkBundle (Boilerplate).
