# Gotenberg — PDF stamp

Adds a text, image or PDF stamp ON TOP OF the page content.
Positioning, rotation, opacity, font via `stampOptions` (JSON).
Reference: `STAMP-DETAIL.md`

Route: `POST /forms/pdfengines/stamp`
Returns: PDF (200) | ZIP (multiple inputs) | 400 | 503
