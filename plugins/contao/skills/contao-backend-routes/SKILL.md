---
name: contao-backend-routes
description: >
  Contao 5 Backend-Routen: AbstractBackendController, Route-Attribut mit _scope=backend,
  Backend-Template (be_main), BackendMenuListener (BACKEND_MENU_BUILD-Event),
  Backend-Assets global und per DCA (TL_CSS/TL_JAVASCRIPT).
  Triggers: contao backend route, contao backend controller, contao AbstractBackendController,
  contao backend menü eintrag, contao custom backend seite, contao backend assets.
---

Lies `references/deep/backend-routes.md` für vollständige Implementierungsdetails
zu eigenen Backend-Routen und Backend-Asset-Integration in Contao 5.
