---
name: panther-browserkit-clients
description: >
  BrowserKit-Clients in Panther als WebDriver-Alternative: HttpBrowser (createHttpBrowserClient)
  fuer HTTP-only Tests ohne echten Browser, Symfony-interner Client (createClient),
  Goutte-Ersatz, Performance-Abwaegung WebDriver vs. BrowserKit, PantherTestCaseTrait
  mit anderen TestCase-Klassen (z.B. LiipFunctionalTestBundle), http_client_options,
  Wann welchen Client waehlen.
  BrowserKit clients as WebDriver alternative: HttpBrowser, Symfony internal client,
  Goutte replacement, performance trade-offs, PantherTestCaseTrait with other test cases.
triggers:
  - panther browserkit
  - panther httpclient
  - panther HttpBrowser
  - panther createHttpBrowserClient
  - panther createClient
  - panther goutte
  - panther ohne browser
  - panther without browser
  - panther http only test
  - panther PantherTestCaseTrait
  - panther LiipFunctionalTestBundle
  - panther webdriver vs browserkit
  - panther client vergleich
  - panther client comparison
  - panther http_client_options
---

# panther-browserkit-clients

BrowserKit-Clients als schnelle Alternative zum WebDriver fuer Tests ohne JavaScript-
Bedarf.

Siehe `references/deep/browserkit-clients.md` fuer vollstaendige Beispiele und
Abwaegungen.
