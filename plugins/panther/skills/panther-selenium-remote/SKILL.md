---
name: panther-selenium-remote
description: >
  Panther mit Selenium: Built-In-Webserver + Selenium Grid, externer Webserver,
  Remote-WebDriver (createSeleniumClient), Proxy-Konfiguration, Self-Signed-SSL
  akzeptieren, Multi-Domain-Anwendungen testen, ChromeDriver-Argumente uebergeben,
  connection_timeout_in_ms / request_timeout_in_ms, DesiredCapabilities.
  Panther with Selenium Grid, remote WebDriver, proxy, self-signed SSL, multi-domain
  apps, ChromeDriver arguments, connection/request timeout options.
triggers:
  - panther selenium
  - panther remote webdriver
  - panther createSeleniumClient
  - panther selenium grid
  - panther externer webserver
  - panther external web server
  - panther proxy
  - panther ssl zertifikat
  - panther ssl certificate
  - panther self-signed ssl
  - panther multi-domain
  - panther chromedriver argumente
  - panther chromedriver arguments
  - panther connection timeout
  - panther request timeout
  - panther DesiredCapabilities
---

# panther-selenium-remote

Selenium Grid, Remote-WebDriver, externer Webserver, Proxy, SSL und Multi-Domain-
Konfiguration mit Panther.

Siehe `references/deep/selenium-remote.md` fuer vollstaendige Beispiele.
