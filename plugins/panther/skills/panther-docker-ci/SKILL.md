---
name: panther-docker-ci
description: >
  Panther in Docker und CI: Dockerfile fuer Chromium und Firefox/GeckoDriver,
  PANTHER_NO_SANDBOX/PANTHER_CHROME_ARGUMENTS fuer Container, Interactive Mode
  (PANTHER_NO_HEADLESS + --debug), vollstaendige CI-Konfigurationen fuer
  GitHub Actions, Travis CI, GitLab CI und AppVeyor, Known Limitations und
  Troubleshooting (Assets im PHP Built-In Server, Bootstrap-Scroll, PANTHER_WEB_SERVER_ROUTER).
  Panther in Docker and CI pipelines with complete YAML for GitHub Actions, Travis,
  GitLab CI, AppVeyor, interactive debug mode, known limitations, troubleshooting.
triggers:
  - panther docker
  - panther dockerfile
  - panther ci
  - panther github actions
  - panther travis ci
  - panther gitlab ci
  - panther appveyor
  - panther interactive mode
  - panther debug mode
  - panther headless ci
  - panther container
  - panther chromium docker
  - panther firefox docker
  - panther known limitations
  - panther troubleshooting
  - panther assets not loading
  - panther bootstrap scroll
  - panther built-in server
---

# panther-docker-ci

Docker-Integration, Interactive Mode und vollstaendige CI-Konfigurationen.

Siehe `references/deep/docker-ci.md` fuer lauffaehige Dockerfiles und CI-YAMLs.
