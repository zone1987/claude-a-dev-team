<!-- generated by scripts/extract_spec.py from openapi.yaml sha256:d7bec97a0a909277 — do not edit above the prose marker -->

# Enumerated values

Every value an OCTO response can carry in a value-like field, and what it means.

Two levels of confidence, kept apart on purpose:

- **Declared** — the specification lists an `enum`. The set is closed: any other value is a protocol violation.
- **Observed** — the specification types the field as a plain string and gives an example. The value is real, but **the set is open**: handle unknown values instead of switching exhaustively.

## Contents

- [Declared enums](#declared-enums)
- [Observed values](#observed-values)

## Declared enums

15 distinct value sets, 55 values. A field name can carry more than one set — `status` on a booking is not `status` on availability — so each set is listed against the schemas it applies to.

### `availabilityType`

On `Product`.

- **START_TIME**: timed slots with their own capacity; availability returns departures.
- **OPENING_HOURS**: open-window admission; availability returns days with opening periods.

### `gateway`

On `CardPayment`, `CardPaymentRequest`.

- **adyen**: processed through Adyen; the `adyen` object carries gateway detail.
- **external**: processed outside OCTO; the `external` object carries the reference.

### `inputType`

On `Question`.

- **text**: single-line free text.
- **textarea**: multi-line free text.
- **select**: one value from a list.
- **radio**: one value from a small set shown together.
- **checkbox**: boolean or multiple selection.
- **date**: a date value.
- **number**: a numeric value.

### `netDiscount` on `Adjustment`

On `Adjustment`.

**A different `netDiscount` enum exists on `Offer`** — the values below are not interchangeable with those.

- **NONE**: the discount does not reduce the net amount; the reseller absorbs it.
- **FULL**: the discount reduces the net amount in full; the supplier absorbs it.
- **SPLIT**: the discount is shared between supplier and reseller.
- **PRORATED**: the discount is distributed proportionally.
- **MANUAL**: the split is set by hand rather than by rule.

### `netDiscount` on `Offer`

On `Offer`.

**A different `netDiscount` enum exists on `Adjustment`** — the values below are not interchangeable with those.

- **NONE**: the discount does not reduce the net amount; the reseller absorbs it.
- **FULL**: the discount reduces the net amount in full; the supplier absorbs it.
- **SPLIT**: the discount is shared between supplier and reseller.
- **PRORATED**: the discount is distributed proportionally.

### `op`

On `WebhookDiffOperation`.

- **add**: the field was added.
- **remove**: the field was removed.
- **replace**: the field's value changed.

### `per`

On `Adjustment`.

- **BOOKING**: applied once for the whole booking.
- **UNIT**: applied once per unit item, so per person or per vehicle.
- **PERCENTAGE**: expressed as a percentage rather than an absolute amount.

### `pricingPer`

On `Product`.

- **UNIT**: applied once per unit item, so per person or per vehicle.
- **BOOKING**: applied once for the whole booking.

### `provider`

On `CardPayment`.

- **adyen**: processed through Adyen; the `adyen` object carries gateway detail.
- **external**: processed outside OCTO; the `external` object carries the reference.

### `refund`

On `BookingCancellation`.

- **FULL**: the discount reduces the net amount in full; the supplier absorbs it.
- **PARTIAL**: part of the amount is refunded, typically after a cutoff.
- **NONE**: the discount does not reduce the net amount; the reseller absorbs it.

### `source`

On `CardPayment`, `CardPaymentRequest`.

- **POS**: card present at a point of sale.
- **ECOM**: online, cardholder not present.
- **MOTO**: mail order or telephone order.

### `status` on `Availability` and 4 more

On `Availability`, `AvailabilityBatchRow`, `AvailabilityCalendar`, `AvailabilityCalendarBatchRow`, `PackageAvailability`.

**A different `status` enum exists on `Booking`** — the values below are not interchangeable with those.

- **AVAILABLE**: capacity confirmed and countable; `vacancies` is meaningful.
- **LIMITED**: sellable but close to capacity; expect `vacancies` to be small.
- **SOLD_OUT**: no capacity left for this slot.
- **FREESALE**: sellable without capacity tracking; `vacancies` and `capacity` may be null.
- **CLOSED**: not operating on this date or slot, regardless of capacity.

### `status` on `Booking`

On `Booking`.

**A different `status` enum exists on `Availability`** — the values below are not interchangeable with those.

- **ON_HOLD**: reserved but not confirmed; capacity is held and the reservation can expire.
- **CONFIRMED**: the sale is complete and the ticket issued.
- **CANCELLED**: actively cancelled, distinct from expiry.
- **EXPIRED**: the reservation timed out before confirmation.
- **REDEEMED**: the ticket was scanned and used.
- **NO_SHOW**: the guest did not arrive; set via the redemption capability.
- **PENDING**: awaiting an external step, typically payment.
- **REJECTED**: the supplier declined the booking.
- **REBOOKED**: superseded by another booking.
- **QUOTE**: priced but never held capacity.

### `waiverPer`

On `Product`.

- **BOOKING**: applied once for the whole booking.
- **UNIT**: applied once per unit item, so per person or per vehicle.

### `weightUnit`

On `AvailabilityUnitRequest`.

- **kg**: kilograms.
- **lb**: pounds.

## Observed values

32 value-like fields the specification does not enumerate. The examples below are the values it documents; more are possible.

### `availableCurrencies`

On `Error`, `Product`. The specification does not enumerate this field, so treat the set as open.

- **EUR**: euro (ISO 4217).
- **USD**: US dollar (ISO 4217).

### `brand`

On `ReusableCard`. The specification does not enumerate this field, so treat the set as open.

- **visa**: Visa card scheme.

### `cancellationCutoffUnit`

On `Option`. The specification does not enumerate this field, so treat the set as open.

- **HOUR**: the amount is counted in hours.

### `country`

On `CheckInLookupRequest`, `Contact`, `MembershipContext`, `MembershipLookupRequest`, `PickupPoint`. The specification does not enumerate this field, so treat the set as open.

- **US**: United States (ISO 3166-1 alpha-2).

### `countryCode`

On `CardPaymentAdyen`. The specification does not enumerate this field, so treat the set as open.

- **US**: United States (ISO 3166-1 alpha-2).

### `currency`

On `AvailabilityBatchRequest`, `AvailabilityCalendarBatchRequest`, `AvailabilityCalendarRequest`, `AvailabilityRequest`, `Booking`, `BookingWriteRequest` and 12 more. The specification does not enumerate this field, so treat the set as open.

- **USD**: US dollar (ISO 4217).

### `defaultCurrency`

On `Product`. The specification does not enumerate this field, so treat the set as open.

- **USD**: US dollar (ISO 4217).

### `deliveryFormat`

On `TicketDeliveryOption`. The specification does not enumerate this field, so treat the set as open.

- **PDF_URL**: a link to a PDF document.

### `deliveryFormats`

On `Product`. The specification does not enumerate this field, so treat the set as open.

- **PDF_URL**: a link to a PDF document.
- **QRCODE**: a QR code payload.

### `deliveryMethod`

On `BookingWriteRequest`. The specification does not enumerate this field, so treat the set as open.

- **VOUCHER**: one ticket media object for the whole booking, at `booking.voucher`.

### `deliveryMethods`

On `Booking`, `Gift`, `Product`. The specification does not enumerate this field, so treat the set as open.

- **TICKET**: one ticket media object per unit item, at `booking.unitItems[].ticket`.
- **VOUCHER**: one ticket media object for the whole booking, at `booking.voucher`.

### `durationUnit`

On `ItineraryItem`, `RentalDuration`. The specification does not enumerate this field, so treat the set as open.

- **HOUR**: the amount is counted in hours.
- **DAY**: the amount is counted in days. Documented in the prose pages, not used as a specification example.

### `environment`

On `CardPaymentAdyen`. The specification does not enumerate this field, so treat the set as open.

- **test**: test mode: consumes no availability, barcodes stay inert, not invoiced.
- **live**: live mode: a real sale. Documented in the prose pages, not used as a specification example.

### `error`

On `Error`. The specification does not enumerate this field, so treat the set as open.

- **BAD_REQUEST**: body malformed, a required field missing, or a wrong data type.

### `errorCode`

On `Error`. The specification does not enumerate this field, so treat the set as open.

- **INVALID_OPTION_ID**: the `optionId` sent does not exist on that product.

### `freesaleDurationRoundupUnit`

On `Product`. The specification does not enumerate this field, so treat the set as open.

- **HOUR**: the amount is counted in hours.

### `freesaleDurationUnit`

On `Product`. The specification does not enumerate this field, so treat the set as open.

- **HOUR**: the amount is counted in hours.

### `locale`

On `Product`. The specification does not enumerate this field, so treat the set as open.

- **en**: English (IETF language tag).

### `locales`

On `Contact`. The specification does not enumerate this field, so treat the set as open.

- **en**: English (IETF language tag).

### `notificationTypes`

On `NotificationSubscription`, `NotificationSubscriptionRequest`. The specification does not enumerate this field, so treat the set as open.

- **STANDARD**: the default variant, with no special handling.

### `redemptionMethod`

On `Product`, `Ticket`. The specification does not enumerate this field, so treat the set as open.

- **DIGITAL**: scanned digitally, for example a QR code.
- **MANIFEST**: no per-guest scan; the guest appears on a manifest list. Documented in the prose pages, not used as a specification example.
- **PRINT**: a printed ticket is checked. Documented in the prose pages, not used as a specification example.

### `resellerStatus`

On `Mapping`. The specification does not enumerate this field, so treat the set as open.

- **CONFIRMED**: the sale is complete and the ticket issued.

### `row`

On `ResourceSeat`. The specification does not enumerate this field, so treat the set as open.

- **A**: a seat or coach row label.

### `settlementMethod`

On `Booking`, `BookingWriteRequest`, `Gift`, `Order`. The specification does not enumerate this field, so treat the set as open.

- **DIRECT**: the reseller settles directly with the supplier for this booking.

### `settlementMethods`

On `Order`, `Product`. The specification does not enumerate this field, so treat the set as open.

- **DEFERRED**: settlement is deferred, typically invoiced periodically rather than per sale.
- **DIRECT**: the reseller settles directly with the supplier for this booking.

### `state`

On `Contact`, `PickupPoint`. The specification does not enumerate this field, so treat the set as open.

- **CA**: California: a subdivision code, not a status value.

### `statuses`

On `CheckInLookupRequest`. The specification does not enumerate this field, so treat the set as open.

- **CONFIRMED**: the sale is complete and the ticket issued.
- **ON_HOLD**: reserved but not confirmed; capacity is held and the reservation can expire.

### `tags`

On `Booking`, `BookingActor`, `BookingWriteRequest`, `Contact`, `Extra`, `Item` and 4 more. The specification does not enumerate this field, so treat the set as open.

- **gratuity**: the item is a gratuity.
- **optional**: the item is optional rather than included.
- **partner**: supplied by a partner.
- **vip**: a VIP variant.

### `travelTimeUnit`

On `ItineraryItem`. The specification does not enumerate this field, so treat the set as open.

- **MINUTE**: the amount is counted in minutes.

### `type`

On `Campaign`, `Item`, `ItineraryItem`, `Unit`. The specification does not enumerate this field, so treat the set as open.

- **ADULT**: an adult guest.
- **PERCENT**: expressed as a percentage.
- **STANDARD**: the default variant, with no special handling.

### `unitType`

On `BookingUnitItem`, `OfferCombinationUnit`, `UnitPricing`. The specification does not enumerate this field, so treat the set as open.

- **ADULT**: an adult guest.
- **CHILD**: a child guest. Documented in the prose pages, not used as a specification example.
- **SENIOR**: a senior guest. Documented in the prose pages, not used as a specification example.

### `verification`

On `CheckInLookupRequest`. The specification does not enumerate this field, so treat the set as open.

- **VERIFIED**: the membership lookup was verified successfully.


## Source

Value sets generated from the Ventrata OCTO `openapi.yaml` 3.0.3, sha256 `d7bec97a0a909277`. Meanings distilled from [docs.ventrata.com](https://docs.ventrata.com) and the specification's own field descriptions; they are verified against the spec on every regeneration.

<!-- prose below this line is written by hand and preserved on regeneration -->
