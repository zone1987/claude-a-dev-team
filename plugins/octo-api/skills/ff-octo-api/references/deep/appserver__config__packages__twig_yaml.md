# config/packages/twig.yaml (`ResubmissionAppServer/config/packages/twig.yaml`)

## Zweck
Twig-Konfiguration: Template-Dateimuster `*.twig`; im `test`-Env `strict_variables: true`.

## Bezüge
`templates/*`, Symfony TwigBundle (Boilerplate).
