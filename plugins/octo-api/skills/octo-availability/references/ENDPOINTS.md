<!-- generated by scripts/extract_spec.py from openapi.yaml sha256:d7bec97a0a909277 — do not edit above the prose marker -->

# Availability endpoints

## Contents

- [`POST /availability`](#post-availability)
- [`POST /availability/batch`](#post-availabilitybatch)
- [`POST /availability/calendar`](#post-availabilitycalendar)
- [`POST /availability/calendar/batch`](#post-availabilitycalendarbatch)
- [`GET /availability/resources`](#get-availabilityresources)
- [`POST /availability/resources`](#post-availabilityresources)


## `POST /availability`

Availability Query.

Returns detailed availability objects per departure time or day. Use this endpoint before booking to obtain the `availabilityId` required by booking creation/reservation requests.

### Request body

`AvailabilityRequest`.

### Responses

- **200**: Successful response. Returns `AvailabilityList`.

## `POST /availability/batch`

Batch Availability Query.

Returns detailed availability across multiple products and options. Designed for background batch jobs and back-office processing, not realtime client requests. For realtime client requests use `POST /availability`.

### Request body

`AvailabilityBatchRequest`.

### Responses

- **200**: Successful response. Returns `AvailabilityBatchList`.

## `POST /availability/calendar`

Availability Calendar Query.

Returns one availability object per date for calendar rendering over a date range. This endpoint is optimized for faster calendar rendering, but it does not replace `POST /availability` for booking flow. You must call `POST /availability` to obtain an `availabilityId` before creating or reserving a booking.

### Request body

`AvailabilityCalendarRequest`.

### Responses

- **200**: Successful response. Returns `AvailabilityCalendarList`.

## `POST /availability/calendar/batch`

Batch Availability Calendar Query.

Returns calendar availability across multiple products/options. Designed for background batch jobs and back-office processing, not realtime client requests. For realtime calendar UX use `POST /availability/calendar`, then call `POST /availability` to obtain the `availabilityId` required for booking.

### Request body

`AvailabilityCalendarBatchRequest`.

### Responses

- **200**: Successful response. Returns `AvailabilityCalendarBatchList`.

## `GET /availability/resources`

Check Available Resources.

Retrieve available resources for a specific availability.

### Responses

- **200**: Successful response. Returns `ResourceAvailabilityList`.

## `POST /availability/resources`

Check Available Resources.

Retrieve available resources for a specific availability.

### Request body

`AvailabilityResourcesRequest`.

### Responses

- **200**: Successful response. Returns `ResourceAvailabilityList`.

<!-- prose below this line is written by hand and preserved on regeneration -->
