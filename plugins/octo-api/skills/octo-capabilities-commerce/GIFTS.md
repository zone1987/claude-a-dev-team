<!-- generated by scripts/extract_spec.py from openapi.yaml sha256:d7bec97a0a909277 — do not edit above the prose marker -->

# `octo/gifts`

Add `octo/gifts` to the `Octo-Capabilities` header to receive these fields.

## Contents

- [`Booking`](#booking)
- [`BookingWriteRequest`](#bookingwriterequest)
- [`Gift`](#gift)
- [`GiftCreateRequest`](#giftcreaterequest)
- [`Order`](#order)
- [`OrderCreateRequest`](#ordercreaterequest)
- [`OrderUpdateRequest`](#orderupdaterequest)
- [Endpoints](#endpoints)
- [Schemas introduced by this capability](#schemas-introduced-by-this-capability)


8 fields across 7 schemas. The fields are additive: the base schemas are unchanged when the capability is absent.

## Endpoints

This capability unlocks 10 endpoint(s).

- **`GET /gifts`**: List Gifts.
  - **resellerReference** (string, in query): Filter by reseller reference.
  - **supplierReference** (string, in query): Filter by supplier reference.
  - **utcCreatedAtStart** (string, in query): UTC ISO8601 range start for created-at filter.
  - **utcCreatedAtEnd** (string, in query): UTC ISO8601 range end for created-at filter.
  - **utcUpdatedAtStart** (string, in query): UTC ISO8601 range start for updated-at filter.
  - **utcUpdatedAtEnd** (string, in query): UTC ISO8601 range end for updated-at filter.
  - **contactEmailAddress** (string, in query): Filter by purchaser email address.
- **`POST /gifts`**: Create Gift. Body `GiftCreateRequest`.
- **`DELETE /gifts/{uuid}`**: Delete Gift. Body `GiftCreateRequest`.
  - **uuid** (string, in path, required): User-submitted UUID field that can be used to uniquely reference this resource and provide idempotency against repeat requests with the same UUID.
  - **reason** (string, in query): Cancellation reason. If not a known enum value, source stores this as cancellation notes with reason `other`.
  - **force** (boolean, in query): Force cancellation when allowed by cancellation rules.
- **`GET /gifts/{uuid}`**: Get Gift.
  - **uuid** (string, in path, required): User-submitted UUID field that can be used to uniquely reference this resource and provide idempotency against repeat requests with the same UUID.
- **`PATCH /gifts/{uuid}`**: Update Gift. Body `GiftCreateRequest`.
  - **uuid** (string, in path, required): User-submitted UUID field that can be used to uniquely reference this resource and provide idempotency against repeat requests with the same UUID.
- **`POST /gifts/{uuid}/cancel`**: Gift Cancellation (Alias). Body `CancelRequest`.
  - **uuid** (string, in path, required): User-submitted UUID field that can be used to uniquely reference this resource and provide idempotency against repeat requests with the same UUID.
- **`POST /gifts/{uuid}/confirm`**: Gift Offers Confirmation. Body `GiftCreateRequest`.
  - **uuid** (string, in path, required): User-submitted UUID field that can be used to uniquely reference this resource and provide idempotency against repeat requests with the same UUID.
- **`POST /gifts/{uuid}/extend`**: Extend Reservation. Body `ExtendRequest`.
  - **uuid** (string, in path, required): User-submitted UUID field that can be used to uniquely reference this resource and provide idempotency against repeat requests with the same UUID.
- **`POST /gifts/{uuid}/notify`**: Notify Gift. Body `NotifyRequest`.
  - **uuid** (string, in path, required): User-submitted UUID field that can be used to uniquely reference this resource and provide idempotency against repeat requests with the same UUID.
- **`PATCH /gifts/{uuid}/preview`**: Gift Offers Preview. Body `GiftCreateRequest`.
  - **uuid** (string, in path, required): User-submitted UUID field that can be used to uniquely reference this resource and provide idempotency against repeat requests with the same UUID.

## Schemas introduced by this capability

3 schema(s) exist only when `octo/gifts` is active.

### `GiftList`

Array of [`Gift`](#gift) — a list response, no fields of its own.

### `GiftPayment`

- **giftCode** (string, optional): Gift code value. Example: `"GIFT-2026-0001"`.
- **code** (string, optional): Code value. Example: `"SUMMER25"`.
- **amount** (number, optional): Amount numeric value. Example: `220.0`.
- **currency** (string, optional): ISO 4217 currency code. Example: `"USD"`.

### `GiftPaymentRequest`

- **code** (string, optional): Code value. Example: `"SUMMER25"`.

## `Booking`

- **giftPayment** (GiftPayment, optional, nullable).

## `BookingWriteRequest`

- **giftPayment** (GiftPayment, optional).

## `Gift`

- **giftPayment** (GiftPayment, optional, nullable).

## `GiftCreateRequest`

- **giftPayment** (GiftPaymentRequest, optional).

## `Order`

- **giftPayment** (GiftPayment, optional, nullable).
- **gifts** (Gift[], optional).

## `OrderCreateRequest`

- **gifts** (GiftCreateRequest[], optional).

## `OrderUpdateRequest`

- **gifts** (GiftCreateRequest[], optional).

## Source

Field list generated from `openapi.yaml` 3.0.3, sha256 `d7bec97a0a909277`. Narrative: [docs.ventrata.com/capabilities/gift-vouchers](https://docs.ventrata.com/capabilities/gift-vouchers).

<!-- prose below this line is written by hand and preserved on regeneration -->

## Integration notes

Behaviour the specification does not carry, from [docs.ventrata.com/capabilities/gift-vouchers](https://docs.ventrata.com/capabilities/gift-vouchers).

### Where the fields appear

Payload paths the documentation names explicitly — useful when mapping a response onto typed objects:

- `gifts[]`
- `gift.updatable`
- `gift.cancellable`
- `voucher.deliveryOptions`

### Values named in the prose

Event names, notification types and enum members the specification does not declare:

- **resellerReference**.
- **supplierReference**.
- **contactEmailAddress**.

### Rules and conditionals

- If the connection fails or you do not receive a response, you can repeat the same request with the same UUID and it will respond with the same gift object without duplicating the gift voucher.
- Note you can only cancel a gift voucher if `gift.cancellable` is true.
- When using this endpoint you must include one of the following filters: * `resellerReference` * `supplierReference` * `utcCreatedAtStart` and `utcCreatedAtEnd` * `utcUpdatedAtStart` and `utcUpdatedAtEnd` * `contactEmailAddress` If no valid filter is provided the API returns `GIFTS_FIELDS_REQUIRED`.
- If the code is valid, currency matches the order currency, and the gift card has available credit then payment is applied.
- If `giftPayment` is present but `code` is omitted/blank, existing non-voided gift payment transactions on the order are voided.

### Delivery formats

Gift voucher delivery URLs are returned directly in `voucher.deliveryOptions` — no separate download
call. The documented formats are **PDF**, **PNG**, **PKPASS** (Apple Wallet) and **Google Wallet**.

`gift.cancellable` and `gift.updatable` say whether a gift may still be changed; read them instead of
inferring from `status`. To redeem a confirmed gift code, add `giftPayment` to a booking, order or
gift write request.
