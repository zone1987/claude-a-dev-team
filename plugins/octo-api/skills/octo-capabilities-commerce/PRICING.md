<!-- generated by scripts/extract_spec.py from openapi.yaml sha256:d7bec97a0a909277 — do not edit above the prose marker -->

# `octo/pricing`

Add `octo/pricing` to the `Octo-Capabilities` header to receive these fields.

## Contents

- [`Availability`](#availability)
- [`AvailabilityBatchRow`](#availabilitybatchrow)
- [`AvailabilityCalendar`](#availabilitycalendar)
- [`AvailabilityCalendarBatchRow`](#availabilitycalendarbatchrow)
- [`Booking`](#booking)
- [`BookingUnitItem`](#bookingunititem)
- [`Error`](#error)
- [`ExtraPricing`](#extrapricing)
- [`Gift`](#gift)
- [`Option`](#option)
- [`Order`](#order)
- [`PackageAvailability`](#packageavailability)
- [`Pricing`](#pricing)
- [`Product`](#product)
- [`Unit`](#unit)
- [`UnitPricing`](#unitpricing)


35 fields across 16 schemas. The fields are additive: the base schemas are unchanged when the capability is absent.

## `Availability`

- **extraPricing** (ExtraPricing[], optional).
- **pricing** (Pricing, optional).
- **unitPricing** (UnitPricing[], optional).

## `AvailabilityBatchRow`

- **extraPricing** (ExtraPricing[], optional).
- **pricing** (Pricing, optional).
- **unitPricing** (UnitPricing[], optional).

## `AvailabilityCalendar`

- **extraPricingFrom** (ExtraPricing[], optional).
- **pricingFrom** (Pricing, optional).
- **unitPricingFrom** (UnitPricing[], optional).

## `AvailabilityCalendarBatchRow`

- **extraPricingFrom** (ExtraPricing[], optional).
- **pricingFrom** (Pricing, optional).
- **unitPricingFrom** (UnitPricing[], optional).

## `Booking`

- **pricing** (Pricing, optional).

## `BookingUnitItem`

- **pricing** (Pricing, optional).

## `Error`

- **availableCurrencies** (string[], optional). Example: `["USD", "EUR"]`.
- **pricing** (Pricing, optional).

## `ExtraPricing`

- **includedTaxes** (IncludedTax[], required).

## `Gift`

- **pricing** (Pricing, optional).

## `Option`

- **pricing** (Pricing, optional).
- **pricingFrom** (Pricing, optional).

## `Order`

- **pricing** (Pricing, optional).

## `PackageAvailability`

- **extraPricing** (ExtraPricing[], optional).
- **pricing** (Pricing, optional).
- **unitPricing** (UnitPricing[], optional).

## `Pricing`

- **includedTaxes** (IncludedTax[], required).

## `Product`

- **availableCurrencies** (string[], optional). Example: `["USD", "EUR"]`.
- **defaultCurrency** (string, optional). Example: `"USD"`.
- **hidePricingFrom** (boolean, optional). Example: `true`.
- **includeTax** (boolean, optional). Example: `true`.
- **includedTaxes** (IncludedTax[], optional).
- **pricingMultiplier** (number, optional). Example: `10.0`.
- **pricingPer** (string, optional). One of: `UNIT`, `BOOKING`. Example: `"UNIT"`.

## `Unit`

- **pricing** (Pricing, optional).
- **pricingFrom** (Pricing, optional).

## `UnitPricing`

- **includedTaxes** (IncludedTax[], required).

## Source

Field list generated from `openapi.yaml` 3.0.3, sha256 `d7bec97a0a909277`. Narrative: [docs.ventrata.com/capabilities/pricing](https://docs.ventrata.com/capabilities/pricing).

<!-- prose below this line is written by hand and preserved on regeneration -->

## Integration notes

Behaviour the specification does not carry, from [docs.ventrata.com/capabilities/pricing](https://docs.ventrata.com/capabilities/pricing).

### Routes this capability extends

- `GET /products`
- `GET /products/{productId}`
- `GET /products/:productId`
- `POST /availability/calendar`
- `GET /availability/calendar`
- `POST /availability`
- `GET /availability`
- `POST /availability/batch`
- `POST /availability/calendar/batch`
- `POST /bookings`
- `PATCH /bookings/{uuid}`
- `POST /bookings/{uuid}/confirm`

### Where the fields appear

Payload paths the documentation names explicitly — useful when mapping a response onto typed objects:

- `availability.pricing`
- `availability.unitPricing[]`
- `option.pricing`
- `option.units[].pricing`
- `option.units[].pricingFrom`
- `unitItems[].pricing`
- `unitItems[].unit.pricing`

### Values named in the prose

Event names, notification types and enum members the specification does not declare:

- **pricing**.
- **availability.pricing**.
- **availability.unitPricing[]**.
- **unitItems[].pricing**.
- **unitItems[].unit.pricing**.

### Rules and conditionals

- Make sure your system is able to support this level of pricing before you integrate with it.
- If `pricingPer = "UNIT"`, each option unit/item includes `pricingFrom`.
- If `pricingPer = "BOOKING"`, each option includes `pricingFrom` instead.
- If `currency` is omitted, pricing arrays include one object per value in `availableCurrencies`.
- If `currency` is set (or set to `default`), pricing arrays contain a single object.
- Throughout this capability, fields ending with `From` are indicative prices and not final prices.
- Make sure this is communicated clearly to customers.
- The documentation above only shows the additions this capability adds to the availability calendar endpoint.
- For `pricingPer = BOOKING`, `pricingFrom` is still returned without unit selection because booking pricing is not ticket-count driven.
- For `pricingPer = BOOKING`, `unitPricingFrom` may still be present for item-style units (type `OTHER`) and can be an empty array.
- We only document the added parameters here, to see the full documentation for the original availability check you can find it here.
- Notice how the response fields are `unitPricing` and `pricing` (without the From suffix).
- Batch variants (`POST /availability/batch` and `POST /availability/calendar/batch`) return the same pricing extensions because they proxy these same endpoints.
- If request `pricing` is provided, it must match the server-calculated booking totals.
- If a partner has enforced price-matching and no matching `pricing` object is sent, the request fails with `PRICING_MATCH_REQUIRED`.

### Callouts

- **Throughout this capability, fields ending with `From` are indicative prices and not final prices. Make sure this is communicated clearly to customers.**
