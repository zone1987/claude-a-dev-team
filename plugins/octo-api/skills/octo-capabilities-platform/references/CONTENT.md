<!-- generated by scripts/extract_spec.py from openapi.yaml sha256:d7bec97a0a909277 — do not edit above the prose marker -->

# `octo/content`

Add `octo/content` to the `Octo-Capabilities` header to receive these fields.

## Contents

- [`Booking`](#booking)
- [`BookingWriteRequest`](#bookingwriterequest)
- [`Extra`](#extra)
- [`Gift`](#gift)
- [`GiftCreateRequest`](#giftcreaterequest)
- [`Option`](#option)
- [`Order`](#order)
- [`OrderCreateRequest`](#ordercreaterequest)
- [`OrderUpdateRequest`](#orderupdaterequest)


14 fields across 9 schemas. The fields are additive: the base schemas are unchanged when the capability is absent.

## `Booking`

- **termsAccepted** (boolean, optional, nullable). Example: `true`.

## `BookingWriteRequest`

- **termsAccepted** (boolean, optional). Example: `true`.

## `Extra`

- **coverImageUrl** (string, optional, nullable). Example: `"https://images.ventrata.com/extras/guide-gratuity.jpg"`.
- **duration** (string, optional, nullable).
- **durationAmount** (number, optional, nullable).
- **durationUnit** (string, optional, nullable).
- **shortDescription** (string, optional). Example: `"Let the guest choose how much they want to tip the guide."`.
- **title** (string, optional). Example: `"Guide Gratuity"`.

## `Gift`

- **termsAccepted** (boolean, optional, nullable). Example: `true`.

## `GiftCreateRequest`

- **termsAccepted** (boolean, optional). Example: `true`.

## `Option`

- **itinerary** (ItineraryItem[], optional).

## `Order`

- **termsAccepted** (boolean, optional, nullable). Example: `true`.

## `OrderCreateRequest`

- **termsAccepted** (boolean, optional). Example: `true`.

## `OrderUpdateRequest`

- **termsAccepted** (boolean, optional). Example: `true`.

## Source

Field list generated from `openapi.yaml` 3.0.3, sha256 `d7bec97a0a909277`. Narrative: [docs.ventrata.com/capabilities/content](https://docs.ventrata.com/capabilities/content).

<!-- prose below this line is written by hand and preserved on regeneration -->

## Integration notes

Behaviour the specification does not carry, from [docs.ventrata.com/capabilities/content](https://docs.ventrata.com/capabilities/content).

### Routes this capability extends

- `GET /products`
- `GET /products/{productId}`
- `POST /bookings`
- `PATCH /bookings/{uuid}`
- `POST /bookings/{uuid}/confirm`
- `POST /orders`
- `PATCH /orders/{orderId}`
- `PATCH /orders/{orderId}/preview`
- `POST /orders/{orderId}/confirm`

### Where the fields appear

Payload paths the documentation names explicitly — useful when mapping a response onto typed objects:

- `itinerary.type`
- `itinerary.travelTime`
- `itinerary.duration`
- `bookings[].termsAccepted`

### Values named in the prose

Event names, notification types and enum members the specification does not declare:

- **itinerary.type**: Enum with one of the following values:
- **START**: The starting or meeting location.
- **POI**: A point of interest visited en route.
- **END**: The ending or final location.
- **itinerary.travelTime**: The travel time required to get to this location.
- **itinerary.duration**: How much time is spent on this itinerary item.
- **title**.
- **shortDescription**.
- **coverImageUrl**.
- **duration**.
- **durationAmount**.
- **durationUnit**.

### Nested object fields

- **notice**: `id`, `title`, `shortDescription`, `coverImageUrl`.
- **tourGroup**: `id`, `internalName`, `title`, `shortDescription`, `icon`.
- **fare**: `id`, `internalName`, `title`, `shortDescription`, `fareGroup`.
- **fareGroup**: `id`, `internalName`, `title`, `shortDescription`.
- **point**: `id`, `internalName`, `title`, `shortDescription`, `pointGroup`.
- **pointGroup**: `id`, `internalName`, `title`, `shortDescription`.
- **font**: `id`, `name`, `normalTtfUrl`, `boldTtfUrl`, `italicTtfUrl`, `boldItalicTtfUrl`.

### Rules and conditionals

- When used with `octo/cart` it also extends Order responses.
- If both are supplied, the order-level `termsAccepted` value takes precedence.
- Base order schema: Multi-Booking Cart. ## Schema Additions (JSON) These are additive fragments showing only fields introduced by this capability. ### `Booking` ### `BookingWriteRequest` ### `Gift` ### `GiftCreateRequest` ### `Option` ### `Order` ### `OrderCreateRequest` ### `OrderUpdateRequest`

### Callouts

- **The content provided by this endpoint belongs to the supplier, so please make sure you have their express permission before using it online.**

### Itinerary item types

`itinerary[].type` distinguishes what a stop is:

- **START** — the starting or meeting location.
- **POI** — a point of interest visited en route.
- **EVENT** — something that happens rather than a place, for example `Lunch` or `Shopping Break`.
- **END** — the ending or final location.

Each item also carries `itinerary.travelTime` (time needed to reach this location) and
`itinerary.duration` (time spent there), each with its own unit field.

### Fields live Ventrata sends that the specification does not declare

Observed on real `octo/content` responses from a Ventrata connection (278 Golden Tours products,
47 Go City). **None of these appear in the OpenAPI document or in this skill's `FIELD-INDEX.json`**,
so a client has to read them defensively — treat each as optional and absent.

| Field | Where | What it holds |
|---|---|---|
| `meetingPoint` | `Option` | free text, the whole of it (see below) |
| `meetingPointLatitude`, `meetingPointLongitude` | `Option` | coordinates, almost never filled |
| `galleryImages` | `Product` | further pictures beside the cover |
| `duration`, `durationAmount`, `durationUnit` | `Option` | how long the experience runs |
| `cancellationCutoff*` | `Option` | declared — see OPTION-SCHEMA.md |

**Duration, meeting point and cancellation cutoff sit on the `Option`, not on the `Product`.** Two
packages of one product routinely differ in all three, so reading them off the product shows the
first option's values for every option.

### `meetingPoint` is one free-text field with no name beside it

There is no separate venue field: the whole address is one string, and the supplier writes the
venue first where there is one — "Westminster Pier, Victoria Embankment, London", "190 High Street,
EH1 1QX, Edinburgh". A client that wants a heading and an address under it has to split the string
itself, on line breaks and commas alike.

**Coordinates are effectively never sent.** Measured on one live Ventrata catalogue: 484 of 705
options name a meeting point and **two** carry `meetingPointLatitude`. Anything drawing a map from
this data has to geocode, and should expect a ceiling well below 100 % — a meeting point is written
the way a guest is told where to go, not the way a gazetteer is searched. "Any Uber Boat Pier
between Putney and Barking Riverside" and "Bus Stop 1 Bullied Way" (the supplier's own typo) have no
coordinate at all.

### The same photograph arrives more than once, under different URLs

Ventrata uploads one picture repeatedly and its CDN gives every upload its own signature, file name
and resolution — so a cover at 800×533 and a gallery entry at 1080×720 of one photograph share not
a single byte. **Comparing URLs cannot detect it and neither can a file hash.** Measured: **85 of
287 products** carried such a pair, showing the same picture twice on one page.

A client that cares has to compare the pictures themselves (a perceptual hash over a downscaled
grey grid separates them cleanly). Sizes are worth knowing about too: 725 KB for a 960×640
photograph is ordinary here, and galleries run to four pictures on average and ten at worst on the
278 products that carry one.

