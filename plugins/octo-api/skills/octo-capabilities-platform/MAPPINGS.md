<!-- generated by scripts/extract_spec.py from openapi.yaml sha256:d7bec97a0a909277 — do not edit above the prose marker -->

# `octo/mappings`

Add `octo/mappings` to the `Octo-Capabilities` header to receive these fields.

## Contents

- [`Availability`](#availability)
- [`AvailabilityBatchRow`](#availabilitybatchrow)
- [`Booking`](#booking)
- [`MappingUpsertRequest`](#mappingupsertrequest)
- [`PackageAvailability`](#packageavailability)
- [Endpoints](#endpoints)
- [Schemas introduced by this capability](#schemas-introduced-by-this-capability)


5 fields across 5 schemas. The fields are additive: the base schemas are unchanged when the capability is absent.

## Endpoints

This capability unlocks 2 endpoint(s).

- **`GET /mappings`**: List Mappings.
  - **optionId** (string, in query, required): Filter by one mapped option ID (`DEFAULT` supported).
  - **resellerReference** (string, in query): Filter by one reseller reference.
  - **resellerStatus** (string, in query): Filter by reseller status.
  - **productId** (string, in query): Filter by one mapped product ID.
  - **productIds[]** (object[], in query): Filter by multiple mapped product IDs.
  - **optionIds[]** (object[], in query): Filter by multiple mapped option IDs (`DEFAULT` supported).
- **`PUT /mappings`**: Update Mappings. Body `MappingUpsertRequest`.

## Schemas introduced by this capability

2 schema(s) exist only when `octo/mappings` is active.

### `Mapping`

Required: `resellerReference`, `optionId`.

- **resellerReference** (string, required): Reseller reference value. Example: `"RES-BOOK-10045"`.
- **resellerStatus** (string, optional, nullable): Reseller status value. Example: `"CONFIRMED"`.
- **resellerProduct** (string, optional, nullable): Reseller product value. Example: `"grand-canyon-day-tour"`.
- **productId** (string, optional, nullable): Product identifier. Example: `"e7cc8bb4-8d1c-4848-8824-5dbedb718681"`.
- **optionId** (string, required, nullable): Option identifier. Example: `"94cdd032-3d32-416d-b0a4-abf8b7495b8b"`.
- **unitId** (string, optional, nullable): Unit identifier. Example: `"unit_adult"`.
- **optionRequired** (boolean, optional): Whether option required is enabled. Example: `true`.
- **unitRequired** (boolean, optional): Whether unit required is enabled. Example: `true`.
- **validityDays** (integer, optional, nullable): Validity days numeric value. Example: `2`.
- **webhookUrl** (string (uri), optional, nullable): Webhook url URL. Example: `"https://www.city-sightseeing.com"`.

### `MappingList`

Array of [`Mapping`](#mapping) — a list response, no fields of its own.

## `Availability`

- **mappings** (Mapping[], optional).

## `AvailabilityBatchRow`

- **mappings** (Mapping[], optional).

## `Booking`

- **mappings** (Mapping[], optional).

## `MappingUpsertRequest`

- **mappings** (Mapping[], optional).

## `PackageAvailability`

- **mappings** (Mapping[], optional).

## Source

Field list generated from `openapi.yaml` 3.0.3, sha256 `d7bec97a0a909277`. Narrative: [docs.ventrata.com/capabilities/mappings](https://docs.ventrata.com/capabilities/mappings).

<!-- prose below this line is written by hand and preserved on regeneration -->

## Integration notes

Behaviour the specification does not carry, from [docs.ventrata.com/capabilities/mappings](https://docs.ventrata.com/capabilities/mappings).

### Routes this capability extends

- `PUT /mappings`

### Where the fields appear

Payload paths the documentation names explicitly — useful when mapping a response onto typed objects:

- `productIds[]`
- `optionIds[]`
- `booking.mappings`
- `availability[].mappings`
- `mappings[]`

### Rules and conditionals

- Body must be a JSON array of mapping rows.
- Whenever a product in your system for this supplier has been changed, updated or deleted, you should repeat this request with all mappings.
- Your response to this request must be `2XX` (for example `200 OK`), otherwise we retry until we receive a `2XX` response. #### Query Parameters If both `productId` and `productIds[]` are provided, `productId` is used.
- If both `optionId` and `optionIds[]` are provided, `optionId` is used.
- Each `mappings[]` item uses the same schema as the mappings list response. ## Schema Additions (JSON) These are additive fragments showing only fields introduced by this capability. ### `Availability` ### `AvailabilityBatchRow` ### `Booking` ### `MappingUpsertRequest` ### `PackageAvailability`

### Filtering by several products

`productIds[]` repeats as a query parameter rather than taking a comma-separated list:

```http
GET /octo/mappings?productIds[]=id1&productIds[]=id2
```

Mappings surface at `booking.mappings`, `availability[].mappings` and in the top-level `mappings[]`
listing, so the same object appears wherever the mapped entity does.
