<!-- generated by scripts/extract_spec.py from openapi.yaml sha256:d7bec97a0a909277 — do not edit above the prose marker -->

# Capability extensions

Fields that appear only when the capability is requested in the `Octo-Capabilities` header. They are additive: the base schema is unchanged.

## Contents

- [`Booking`](#booking)
- [`BookingUnitItem`](#bookingunititem)


## `Booking`

45 fields across 17 capabilities.

### octo/adjustments

Add `octo/adjustments` to the `Octo-Capabilities` header.

- **adjustments** (Adjustment[], optional).

### octo/cart

Add `octo/cart` to the `Octo-Capabilities` header.

- **orderId** (string, optional, nullable). Example: `"ord_20260514_9f3c21"`.
- **orderInvoicePdfUrl** (string, optional, nullable). Example: `"https://www.city-sightseeing.com"`.
- **orderReference** (string, optional, nullable). Example: `"ORDER-2026-0514-1001"`.
- **primary** (boolean, optional). Example: `true`.

### octo/checkin

Add `octo/checkin` to the `Octo-Capabilities` header.

- **checkedIn** (boolean, optional). Example: `true`.
- **checkinAvailable** (boolean, optional). Example: `true`.
- **checkinUrl** (string, optional, nullable). Example: `"https://checkin.city-sightseeing.com/booking/89fe0192"`.

### octo/content

Add `octo/content` to the `Octo-Capabilities` header.

- **termsAccepted** (boolean, optional, nullable). Example: `true`.

### octo/extras

Add `octo/extras` to the `Octo-Capabilities` header.

- **extraItems** (ExtraItem[], optional).

### octo/gifts

Add `octo/gifts` to the `Octo-Capabilities` header.

- **giftPayment** (GiftPayment, optional, nullable).

### octo/identities

Add `octo/identities` to the `Octo-Capabilities` header.

- **identity** (IdentitySummary, optional, nullable).
- **identityId** (string, optional, nullable). Example: `"identity_customer_001"`.

### octo/mappings

Add `octo/mappings` to the `Octo-Capabilities` header.

- **mappings** (Mapping[], optional).

### octo/memberships

Add `octo/memberships` to the `Octo-Capabilities` header.

- **appendMembershipValidity** (boolean, optional). Example: `true`.
- **membership** (Membership, optional, nullable).

### octo/offers

Add `octo/offers` to the `Octo-Capabilities` header.

- **offerCode** (string, optional, nullable). Example: `"SUMMER25"`.
- **offerComparisons** (OfferComparison[], optional).
- **offerIsCombination** (boolean, optional). Example: `true`.
- **offerTitle** (string, optional, nullable). Example: `"Summer Promotion"`.

### octo/packages

Add `octo/packages` to the `Octo-Capabilities` header.

- **packageIncludeId** (string, optional, nullable). Example: `"package_include_id-example"`.
- **packageUnitItemUuid** (string, optional, nullable). Example: `"89fe0192-ddcd-430a-b285-e1396a4725d2"`.
- **packageUuid** (string, optional, nullable). Example: `"89fe0192-ddcd-430a-b285-e1396a4725d2"`.

### octo/pickups

Add `octo/pickups` to the `Octo-Capabilities` header.

- **dropoffDispatch** (Dispatch, optional, nullable).
- **dropoffHotel** (string, optional, nullable): Resolved dropoff hotel name and address. Example: `"Hotel Riu Plaza Fisherman's Wharf, San Francisco, CA"`.
- **dropoffNotes** (string, optional, nullable). Example: `"Drop-off at Fisherman's Wharf."`.
- **dropoffPoint** (PickupPoint, optional, nullable).
- **dropoffPointId** (string (uuid), optional, nullable): Selected dropoff point UUID. Example: `"8d7a2fd8-f8f8-4e61-bbaf-5adac58f5d46"`.
- **dropoffRequested** (boolean, optional). Example: `true`.
- **pickupDispatch** (Dispatch, optional, nullable).
- **pickupHotel** (string, optional, nullable): Resolved pickup hotel name and address. Example: `"Hilton Union Square, 333 O'Farrell St, San Francisco, CA 94102"`.
- **pickupHotelRoom** (string, optional, nullable). Example: `"1208"`.
- **pickupNotes** (string, optional, nullable). Example: `"Meet the guide by the hotel lobby."`.
- **pickupPoint** (PickupPoint, optional, nullable).
- **pickupPointId** (string (uuid), optional, nullable): Selected pickup point UUID. Example: `"1cfe2c68-4a91-4df8-8d69-4b393d469caf"`.
- **pickupRequested** (boolean, optional). Example: `true`.

### octo/pricing

Add `octo/pricing` to the `Octo-Capabilities` header.

- **pricing** (Pricing, optional).

### octo/questions

Add `octo/questions` to the `Octo-Capabilities` header.

- **questionAnswers** (QuestionAnswer[], optional).

### octo/rentals

Add `octo/rentals` to the `Octo-Capabilities` header.

- **rentalDuration** (RentalDuration, optional, nullable).
- **rentalDurationId** (string, optional, nullable). Example: `"rental_duration_2h"`.

### octo/resources

Add `octo/resources` to the `Octo-Capabilities` header.

- **resourceAllocations** (ResourceAllocation[], optional).

### octo/waivers

Add `octo/waivers` to the `Octo-Capabilities` header.

- **waiverFieldValues** (WaiverFieldValue[], optional).
- **waiverFileUrl** (string (uri), optional, nullable): URL to the stored waiver file. Example: `"https://api.ventrata.com/uploads/waivers/waiver_booking_10045.pdf"`.
- **waiverId** (string (uuid), optional, nullable): Selected waiver template UUID. Example: `"6d8c49eb-d6d3-4f03-a242-6f5f95f4d56f"`.
- **waiversComplete** (boolean, optional): Whether waiver requirements are complete for this booking. Example: `true`.

## `BookingUnitItem`

14 fields across 7 capabilities.

### octo/extras

Add `octo/extras` to the `Octo-Capabilities` header.

- **extraItems** (ExtraItem[], optional).

### octo/offers

Add `octo/offers` to the `Octo-Capabilities` header.

- **offerCode** (string, optional, nullable). Example: `"SUMMER25"`.
- **offerComparisons** (OfferComparison[], optional).
- **offerIsCombination** (boolean, optional). Example: `true`.
- **offerTitle** (string, optional, nullable). Example: `"Summer Promotion"`.

### octo/packages

Add `octo/packages` to the `Octo-Capabilities` header.

- **packageIncludeId** (string, optional, nullable). Example: `"package_include_id-example"`.
- **packageUnitItemUuid** (string, optional, nullable). Example: `"89fe0192-ddcd-430a-b285-e1396a4725d2"`.

### octo/pricing

Add `octo/pricing` to the `Octo-Capabilities` header.

- **pricing** (Pricing, optional).

### octo/questions

Add `octo/questions` to the `Octo-Capabilities` header.

- **questionAnswers** (QuestionAnswer[], optional).

### octo/redemption

Add `octo/redemption` to the `Octo-Capabilities` header.

- **scans** (RedemptionScan[], optional).

### octo/waivers

Add `octo/waivers` to the `Octo-Capabilities` header.

- **waiverFieldValues** (WaiverFieldValue[], optional).
- **waiverFileUrl** (string (uri), optional, nullable): URL to the stored waiver file. Example: `"https://api.ventrata.com/uploads/waivers/waiver_booking_10045.pdf"`.
- **waiverId** (string (uuid), optional, nullable): Selected waiver template UUID. Example: `"6d8c49eb-d6d3-4f03-a242-6f5f95f4d56f"`.
- **waiversComplete** (boolean, optional): Whether waiver requirements are complete for this unit item. Example: `true`.

<!-- prose below this line is written by hand and preserved on regeneration -->
