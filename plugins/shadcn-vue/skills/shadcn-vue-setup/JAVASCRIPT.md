# shadcn-vue: JavaScript

shadcn-vue is written in TypeScript, but also provides JavaScript versions
of the components. Opting out is done via the `typescript` flag in
`components.json`.

## Reference Files

- `JAVASCRIPT-DETAIL.md` — typescript: false in components.json (complete
  example), jsconfig.json for the @/* path alias, CLI behavior in JS projects
