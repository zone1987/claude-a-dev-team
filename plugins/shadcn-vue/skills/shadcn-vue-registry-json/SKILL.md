---
name: shadcn-vue-registry-json
description: >
  Vollstaendiges Schema der registry.json fuer shadcn-vue Custom Registries: Felder $schema, name,
  homepage, items. Jedes Item muss dem registry-item-Schema entsprechen. JSON-Beispiele fuer alle
  Felder.
  Complete registry.json schema for shadcn-vue custom registries: $schema, name, homepage, items
  fields with full JSON examples. Each item must conform to the registry-item schema specification.
triggers:
  - registry.json schema
  - registry json format
  - registry name homepage
  - registry items array
  - shadcn registry schema
---

# shadcn-vue registry.json Schema

Vollstaendige Schema-Dokumentation fuer `registry.json`.

## Referenzen

- [registry-json.md](references/registry-json.md) — Alle Felder mit JSON-Beispielen:
  $schema, name, homepage, items

Quelle: `/tmp/shadcn-vue-repo/apps/v4/content/docs/registry/registry-json.md`
