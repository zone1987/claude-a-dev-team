---
name: shadcn-vue-cli
description: >
  shadcn-vue CLI vollstaendige Referenz: alle Commands init/create, add, apply, view,
  search/list, build, docs, info, migrate (rtl, icons) mit saemtlichen Flags und Optionen.
  Triggers: "shadcn vue cli", "npx shadcn-vue", "shadcn vue init", "shadcn vue add",
  "shadcn vue build", "shadcn vue migrate", "shadcn cli befehle", "shadcn vue flags",
  "shadcn vue apply preset", "shadcn vue search", "shadcn vue view", "shadcn cli optionen",
  "shadcn vue docs command", "shadcn vue info", "migrate rtl shadcn"
---

# shadcn-vue: CLI

Der shadcn-vue CLI ist das primaere Werkzeug zum Verwalten von Komponenten,
Presets und Registries.

```bash
npx shadcn-vue@latest <command>
```

## Reference Files

- `references/cli.md` — Alle Commands vollstaendig mit saemtlichen Flags:
  `init` (create Alias, --preset, --template, --style, --icon-library, --font,
  --base-color, --name, --defaults, --yes, --force, --cwd, --silent, --src-dir,
  --reinstall, --rtl, --pointer, --css-variables, --no-base-style),
  `add` (-y, -o, -c, -a, -p, -s),
  `apply` (--preset, -y, -c, -s),
  `view` (mehrere Items, namespaced Registries),
  `search`/`list` (-c, -q, -l, -o),
  `build` (-o, -c),
  `docs` (-c, -b, --json),
  `info` (-c, --json),
  `migrate` [icons|rtl] (-c, -l, -y), migrate rtl Detail
  (Dateien und Glob-Pattern)
