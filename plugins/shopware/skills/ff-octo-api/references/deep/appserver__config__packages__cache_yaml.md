# config/packages/cache.yaml (`ResubmissionAppServer/config/packages/cache.yaml`)

## Zweck
Symfony-Cache-Konfiguration (Standard/Boilerplate; App-Cache-Pools). Keine OCTO-spezifische Bedeutung.

## Bezüge
Symfony FrameworkBundle (Boilerplate).
