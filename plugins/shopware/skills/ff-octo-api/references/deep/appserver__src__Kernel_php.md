# Kernel (`ResubmissionAppServer/src/Kernel.php`)

## Zweck
Symfony-MicroKernel des AppServers (Standard-Setup).

## Typ & Vererbung
- Namespace `App`, `class Kernel extends BaseKernel`, nutzt `MicroKernelTrait`.

## Bezüge
`config/*`, Symfony-Standard.
