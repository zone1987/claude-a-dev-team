# config/packages/routing.yaml (`ResubmissionAppServer/config/packages/routing.yaml`)

## Zweck
Symfony-Routing-Konfiguration (z.B. `strict_requirements` je Env). Standard/Boilerplate.

## Bezüge
`config/routes*.yaml`, Symfony Routing (Boilerplate).
