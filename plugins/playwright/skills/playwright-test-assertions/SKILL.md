---
name: playwright-test-assertions
description: >
  Alle Playwright expect-Matcher vollstaendig: Locator-Assertions (auto-retrying),
  Page-Assertions, APIResponse-Assertions, Generic-Assertions (nicht-retrying),
  Snapshot-Assertions (toHaveScreenshot, toMatchSnapshot), Soft-Assertions, expect.poll,
  expect.toPass, expect.extend, asymmetrische Matcher. Playwright assertions, expect matchers,
  toBeVisible, toHaveText, toHaveScreenshot, soft assertions.
triggers:
  - playwright assertions
  - expect playwright
  - toBeVisible
  - toHaveText
  - toHaveScreenshot
  - toMatchSnapshot
  - soft assertions playwright
  - expect.soft
  - expect.poll
  - expect.toPass
  - playwright matcher
  - playwright snapshot
  - playwright screenshot vergleich
  - playwright visual regression
  - toBeChecked
  - toHaveValue
  - toHaveURL
  - toBeOK
  - expect.extend playwright
  - playwright custom matcher
---

Vollstaendige Assertion-Referenz: alle `expect(locator).*`-Matcher (auto-retry),
`expect(page).*`, `expect(response).*`, alle generischen Matcher, Snapshot-Assertions
mit allen Optionen, Soft-Assertions, `expect.poll`, `expect.toPass`, `expect.extend`.

## Vertiefung

- [references/deep/assertions.md](references/deep/assertions.md) — Alle Matcher als Tabelle mit Typen und Beispielen
