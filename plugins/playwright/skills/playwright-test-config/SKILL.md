---
name: playwright-test-config
description: >
  playwright.config.ts vollstaendig: alle Top-Level-Optionen, alle use-Optionen (Browser,
  Netzwerk, Emulation, Recording), Projects/Dependencies, TypeScript-Setup (tsconfig,
  paths, tsconfig-Option). Playwright test configuration, use options, projects, TypeScript.
triggers:
  - playwright.config
  - playwright config
  - playwright konfigurieren
  - playwright configure
  - defineConfig
  - use options
  - testDir
  - fullyParallel
  - forbidOnly
  - baseURL
  - storageState
  - playwright projects
  - project dependencies
  - playwright typescript
  - playwright tsconfig
  - playwright path aliases
  - playwright webServer config
  - expect.timeout config
  - playwright reporter config
  - playwright workers config
  - playwright timeout config
---

Konfigurationsdatei `playwright.config.ts` mit `defineConfig()`. Vollstaendige Referenz
aller Top-Level-Optionen, aller `use`-Optionen (Browser, Netzwerk, Emulation, Recording),
Projects inkl. Dependencies/Teardown und TypeScript-Integration.

## Vertiefung

- [references/deep/config.md](references/deep/config.md) — Alle Top-Level + use-Optionen als Tabelle
