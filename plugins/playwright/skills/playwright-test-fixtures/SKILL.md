---
name: playwright-test-fixtures
description: >
  Playwright Fixtures vollstaendig: eingebaute Fixtures (page, context, browser, request),
  test.extend() fuer eigene Fixtures, Worker- vs. Test-Scope, auto/option-Fixtures,
  mergeTests, Fixture-Timeout, globalSetup/Teardown, Parametrisierung. Playwright fixtures,
  test.extend, worker scope, global setup teardown, parameterize tests.
triggers:
  - playwright fixtures
  - test.extend
  - playwright fixture
  - worker fixture
  - auto fixture
  - option fixture
  - mergeTests
  - playwright global setup
  - globalSetup
  - globalTeardown
  - playwright setup teardown
  - playwright parameterize
  - parameterized tests playwright
  - test parametrisierung
  - playwright csv tests
  - playwright test daten
  - fixture scope
  - playwright beforeAll
  - playwright afterAll
  - playwright beforeEach
---

Playwright-Fixtures: eingebaute Fixtures, `test.extend()` fuer eigene Test-/Worker-Fixtures,
auto/option-Fixtures, Fixture-Komposition, `mergeTests()`, globalSetup/Teardown (Empfehlung:
Project Dependencies), Parametrisierung ueber `forEach`, Fixture-Optionen und CSV.

## Vertiefung

- [references/deep/fixtures.md](references/deep/fixtures.md) — Vollstaendige Fixture-Referenz mit allen Optionen
