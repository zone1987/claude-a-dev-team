# playwright-test-fixtures

Playwright fixtures: built-in fixtures, `test.extend()` for custom test/worker fixtures,
auto/option fixtures, fixture composition, `mergeTests()`, globalSetup/Teardown (recommendation:
project dependencies), parametrization via `forEach`, fixture options and CSV.

## Deep dive

- [TEST-FIXTURES-FIXTURES.md](TEST-FIXTURES-FIXTURES.md) — Complete fixture reference with all options
