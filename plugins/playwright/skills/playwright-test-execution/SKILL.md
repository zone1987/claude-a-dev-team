---
name: playwright-test-execution
description: >
  Playwright Test-Ausfuehrung vollstaendig: alle CLI-Flags (npx playwright test),
  Parallelitaet (workers, fullyParallel, serial), Sharding mit Blob-Reporter, Retries
  (Konfiguration, Status, testInfo.retry), alle Timeout-Arten (Test, Expect, Action,
  Navigation, Global, Fixture), UI-Mode, webServer. Playwright CLI, parallel, shard,
  retry, timeout, UI mode, web server.
triggers:
  - playwright cli
  - npx playwright test
  - playwright ausfuehren
  - playwright run tests
  - playwright --headed
  - playwright --debug
  - playwright --workers
  - playwright parallel
  - fullyParallel
  - playwright serial
  - playwright sharding
  - playwright shard
  - playwright retries
  - playwright retry
  - playwright timeout
  - actionTimeout
  - navigationTimeout
  - globalTimeout
  - playwright UI mode
  - playwright --ui
  - playwright webServer
  - playwright --last-failed
  - playwright --grep
  - playwright --project
  - playwright --update-snapshots
---

Alle CLI-Optionen, Parallelitaet, Sharding, Retries und Timeout-Typen.
Vollstaendige Flags-Tabelle, `webServer`-Konfiguration und UI-Mode-Features.

## Vertiefung

- [references/deep/execution.md](references/deep/execution.md) — CLI-Flags-Tabelle, Parallelitaet, Sharding, Retries, Timeouts, UI-Mode
