---
name: sw-config-reference
description: >
  Shopware 6 Webserver-Konfiguration — vollständige Referenz-Configs für Nginx, Apache und Caddy.
  Trigger: "nginx config shopware", "apache vhost shopware", "caddy shopware config",
  "DocumentRoot shopware", "webserver config referenz", "nginx location config",
  "server config shopware", "webserver vollständig konfigurieren". Shopware 6.7.
---

# Shopware 6 — Server-Konfigurationsreferenz

Vollständige Webserver-Konfigurationen für Nginx, Apache und Caddy.
Document Root zeigt immer auf den `public/`-Ordner.

Ausführliche Configs: `references/deep/server-configs.md`
